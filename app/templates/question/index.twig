{% extends 'layout/site.twig' %}

{% block content %}
    {#
    some question detailsrrrr
    {% set answers = craft.answer.find({
        relatedTo: entry
    }) %}

    {{ answers|length }}<br>
    {% for answer in answers %}
        {{ answer.title }}<br>
    {% endfor %}
    #}
    ee
    <pre>
    {{ dump(entry.test_id.ids()) }}
    </pre>

    <br><br><br>
    {% for answer in entry.test_id %}
        {{ answer.title }}
        {{ answer.id }}<br>
    {% endfor %}
    <br><br>
    {% set pages = entry.pages.find() %}
    {% if pages %}
        {% for page in pages %}
            {{ page.title }}<br>
        {% endfor %}
    {% endif %}

    {% set form = anu.question.renderForm(entry) %}
    {{ dump(form) }}
    {{ form|raw }}
{% endblock %}