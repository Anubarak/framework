{% extends 'layout/site.twig' %}

{% block content %}
    {{ entry.title }}

    {% for matrix in entry.matrix %}
        {{ matrix.id }}<br>
        {{ dump(matrix.content) }}
        {{ matrix.text }}
    {% endfor %}
{% endblock %}