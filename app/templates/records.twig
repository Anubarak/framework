{% extends 'layout/site.twig' %}

{% block content %}
    {{ test }}
    {% if records is defined and records is not null %}
        {% for record in records %}
            {{ record.getTableName() }}
        {% endfor %}
    {% endif %}
    {% includeJsFile 'ein File'%}
    {% includeJsFile 'ein File2efefe'%}
    {{ includeJsFile }}
{% endblock %}