{% extends 'layout/site.twig' %}

{% block content %}
    {#
    some question detailsrrrr
    {% set answers = craft.answer.find({
        relatedTo: entry
    }) %}

    {{ answers|length }}<br>
    {% for answer in answers %}
        {{ answer.title }}<br>
    {% endfor %}
    #}

    {% set form = anu.answer.renderForm(entry) %}
    {{ form|raw }}

{% endblock %}