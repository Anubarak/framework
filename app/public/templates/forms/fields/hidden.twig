
<input type="hidden"
       id="{{ class }}{{ entry.id }}{{ key }}"
       ng-model="data.{{ key }}"
       name="data[{{ key }}]"
       value="{{ attribute(entry, key) }}"
       class="form-control"
       string-to-number
>